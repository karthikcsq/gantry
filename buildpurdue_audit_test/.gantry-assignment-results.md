# Gantry validation — manual quiz results

Repo: buildpurdue-website (Next.js / TS)
Date: 2026-05-17
Sampled from: 418 load-bearing functions (1212 total)

---

## Q1. `loadAuthUserData`

- File: `src/app/apply/page.tsx:390`
- Signature: ` const loadAuthUserData = async ()`
- LOC: 66 | cross-imports: 0 | test-refs: 0

**Prediction:** it gets the users supabase data, like name, approval status, etc.

**Actual behavior:** Fetches `/api/auth/status`, then `/api/application/check-email`. Branches: approved users get read-only form populated; pending users get router.push redirect; otherwise prefills email/name and loads draft answers, then advances form to `has_draft` state.

**Self-score:** PARTIAL

**What I was wrong about:** "I got the right result, but didn't know about the api or where it was used"

**Did not recognize this existed:** no

---

## Q2. `validatePostedUrl`

- File: `src/components/admin/posting/PostedUrlInput.tsx:14`
- Signature: `export function validatePostedUrl(platform: Platform, raw: string): 'empty' | 'valid' | 'invalid'`
- LOC: 8 | cross-imports: 3 | test-refs: 0

**Prediction:** It checks a posts copy for valid links before conversion to the tracked links

**Actual behavior:** Looks up the platform's workflow config; if `requiresPostedUrl === false`, returns `valid`. Otherwise trims input — empty → `empty`, missing pattern → `valid`, else tests `cfg.postUrlPattern` regex. Validates a URL the admin pastes back after publishing a post (publish-confirmation flow), used by `PostedUrlInput` for border color and hint text.

**Self-score:** WRONG

**What I was wrong about:** "I didn't develop this, and I have no idea what it does"

**Did not recognize this existed:** yes

---

## Q3. `GET` (campaign analytics list)

- File: `src/app/api/admin/analytics/campaigns/route.ts:6`
- Signature: `export async function GET(request: NextRequest)`
- LOC: 86 | cross-imports: 0 | test-refs: 0

**Prediction:** it gets all the analytics for a particular campaign (funnel data, tracked link clicks, etc)

**Actual behavior:** Admin-gated GET returning analytics for **all** non-template campaigns (not one). Parses `from`/`to`/`includeArchived` query params (default 7-day window); for each campaign counts non-bot clicks and runs raw SQL `GROUP BY DATE` for a daily sparkline. No funnel data — that's the separate `/funnel` route.

**Self-score:** PARTIAL

**What I was wrong about:** "I did know it existed, but not specifically that it did what you said" — predicted single-campaign + funnel data; actually all-campaigns list with clicks + sparkline only.

**Did not recognize this existed:** no

---

## Q4. `handleDelete`

- File: `src/app/admin/db/EmailDatabaseClient.tsx:719`
- Signature: ` const handleDelete = async ()`
- LOC: 22 | cross-imports: 0 | test-refs: 0

**Prediction:** It deletes the selected email/user from the admin database table.

**Actual behavior:** Bulk-DELETEs selected contacts via `/api/admin/contacts/delete`. On success: shows feedback for 1.5s, closes confirm modal, clears selection, awaits `onRefresh()`. Errors swallowed silently.

**Self-score:** RIGHT

**What I was wrong about:** n/a

**Did not recognize this existed:** no

---

## Q5. `DELETE` (staff draft)

- File: `src/app/api/staff/draft/route.ts:81`
- Signature: `export async function DELETE(request: NextRequest)`
- LOC: 31 | cross-imports: 0 | test-refs: 0

**Prediction:** delete a draft of a staff application

**Actual behavior:** Reads `email`/`roleId` from query params (400 if missing), lowercases email, calls `prisma.staffApplicationDraft.deleteMany` keyed on email + roleId. Uses `deleteMany` to handle not-found gracefully. No auth check — lookup keyed on email/roleId pair.

**Self-score:** RIGHT

**What I was wrong about:** n/a

**Did not recognize this existed:** no

---

## Q6. `Footer`

- File: `src/components/Footer.tsx:3`
- Signature: `export function Footer()`
- LOC: 25 | cross-imports: 1 | test-refs: 0

**Prediction:** footer at the bottom of the website

**Actual behavior:** Renders `<footer>` with copyright (UTC year) + Terms / Privacy Policy links.

**Self-score:** RIGHT

**What I was wrong about:** n/a

**Did not recognize this existed:** no

---

## Q7. `handleCheckboxChange`

- File: `src/components/events/CheckinForm.tsx:179`
- Signature: ` const handleCheckboxChange = (questionId: string, option: string, checked: boolean)`
- LOC: 17 | cross-imports: 0 | test-refs: 0

**Prediction:** updated checkbox for the check in form for autosave purposes

**Actual behavior:** Toggles the option in the question's answer array; then recomputes visible questions via `getVisibleQuestions` and **deletes answers for any question that became hidden** due to dependency changes. Pure local state; no autosave call.

**Self-score:** WRONG

**What I was wrong about:** "it was for the dependency graph" — predicted autosave purpose; actual purpose is conditional-question dependency cleanup.

**Did not recognize this existed:** no

---

## Q8. `CohostSection`

- File: `src/components/admin/CohostSection.tsx:19`
- Signature: `export default function CohostSection({ entityId, entityType }: CohostSectionProps)`
- LOC: 0 | cross-imports: 2 | test-refs: 0

**Prediction:** no idea

**Actual behavior:** Reusable admin component (event or program) for cohost management. GETs the cohosts list on mount, renders list with remove buttons, includes add-cohost form (email + first/last) POSTing to `/api/admin/{events|programs}/{id}/cohosts`. Handles 201 (append + toast), 409 (duplicate), and other errors. Remove DELETEs and optimistically filters. Per-row removing state tracked.

**Self-score:** WRONG

**What I was wrong about:** "no I didn't know this existed"

**Did not recognize this existed:** yes

---

## Q9. `normalizeParsedState`

- File: `src/hooks/usePostingWorkflowState.ts:173`
- Signature: `function normalizeParsedState(parsed: Partial<PostingWorkflowState>): PostingWorkflowState`
- LOC: 56 | cross-imports: 0 | test-refs: 0

**Prediction:** This turns the json of the posting workflow into a state that the user can immediately jump to on the platform

**Actual behavior:** Defensive schema migration + defaults: strips legacy `linkOverrides`, migrates draft views via `migrateDraftViewsFromCheckpoint`, validates `lastDraftView`/`furthestDraftView`/`lastPublishPlatform` against enums, coerces `furthestPublishPlatformIndex`/`amplifySlackUsedCombinedCopyOpen` to safe defaults, deep-merges `stage1` (including nested checklists/approvals) with `DEFAULT_WORKFLOW_STATE`, migrates `stage2` via `migrateStage2FromParsed`. Output is always a complete valid state.

**Self-score:** PARTIAL (user offered RIGHT or PARTIAL; called PARTIAL — migration + validation is the load-bearing work, not a detail)

**What I was wrong about:** "i missed a lot of the error checking, but honestly was correct"

**Did not recognize this existed:** no

---

## Q10. `GET` (funnel analytics)

- File: `src/app/api/admin/analytics/funnel/route.ts:8`
- Signature: `export async function GET(request: NextRequest)`
- LOC: 76 | cross-imports: 0 | test-refs: 0

**Prediction:** This is the api that gets the funnel data for a specific campaign

**Actual behavior:** Admin-gated, site-wide (no campaign filter). Date range via `from`/`to` (default 7 days). Raw SQL `GROUP BY "eventType"` against `funnel_events`. Computes counts for PAGE_LOAD / EMAIL_ENTERED / FORM_SUBMITTED, three conversion rates (load→email, email→submit, overall), and two drop-off counts. Returns one funnel object.

**Self-score:** PARTIAL

**What I was wrong about:** "i didn't get the scope right" — assumed campaign-scoped; actually site-wide aggregate. Same scope-mismatch pattern as Q3.

**Did not recognize this existed:** no

---

# REPORT

## 1. Score summary
- **RIGHT:** 3 (Q4 `handleDelete`, Q5 staff-draft `DELETE`, Q6 `Footer`)
- **PARTIAL:** 4 (Q1 `loadAuthUserData`, Q3 campaigns-analytics `GET`, Q9 `normalizeParsedState`, Q10 funnel-analytics `GET`)
- **WRONG:** 3 (Q2 `validatePostedUrl`, Q7 `handleCheckboxChange`, Q8 `CohostSection`)

## 2. Wrong-prediction rate
**70%** (7 of 10 — PARTIAL counts as wrong for this gate)

## 3. Didn't-recognize rate
**20%** (2 of 10 — Q2 `validatePostedUrl`, Q8 `CohostSection`)

## 4. Verdict against calibration premise
**CALIBRATION PREMISE VALIDATED** (wrong-rate 70% ≥ 40%). Self-report is signal-bearing. The user genuinely cannot predict load-bearing function behavior from name + signature alone on the majority of sampled functions in this AI-shipped codebase. → Proceed to /plan-eng-review.

## 5. Verdict against hidden-value premise
**MARGINAL — falls in the dead zone between thresholds** (20% is below the 30% "validated" bar but above the 10% "weak" bar). Two of ten sampled load-bearing functions were unrecognized — non-trivial, but not strong evidence on its own. Recommend re-running on a second repo (ideally older or with more contributor churn) before committing to the hidden-value framing in launch copy.

A secondary signal worth noting: of the 8 functions the user *did* recognize, 4 still got PARTIAL or WRONG scores (Q1, Q3, Q7, Q10). The hidden-value premise may need to be reframed: gantry's audit doesn't only surface unknown functions — it surfaces **functions the user recognizes but misunderstands**, which is arguably the more dangerous failure mode.

## 6. Surprising / rebuild-candidate functions
**Functions marked "didn't recognize":**
- `src/components/admin/posting/PostedUrlInput.tsx:14` — `validatePostedUrl` (Q2)
- `src/components/admin/CohostSection.tsx:19` — `CohostSection` (Q8)

**Load-bearing functions the user got WRONG (the rebuild candidates that justify gantry's existence):**
- `src/components/admin/posting/PostedUrlInput.tsx:14` — `validatePostedUrl` (Q2)
- `src/components/events/CheckinForm.tsx:179` — `handleCheckboxChange` (Q7) — predicted "autosave"; actually conditional-question dependency cleanup. Wrong about *purpose*, not just details.
- `src/components/admin/CohostSection.tsx:19` — `CohostSection` (Q8)

**Load-bearing functions where the user was PARTIAL on scope/structure (also rebuild-relevant):**
- Q3 + Q10 (both analytics route handlers): user assumed campaign-scoped on aggregate endpoints. **Repeated scope-mismatch pattern.** Suggests a mental model of the analytics subsystem that's one level too granular.
- Q9 `normalizeParsedState`: user understood input/output framing but missed the migration + validation work, which is the function's actual reason for existing.

## 7. Notes for the gantry design doc
- The 70% wrong-rate is decisive even with the small sample (n=10). Worth re-running on at least one more repo to confirm distribution stability before finalizing copy.
- The "scope-mismatch" pattern (Q3 + Q10) is interesting: it's a class of error a quiz can surface but a naive `is this function familiar?` UX cannot. Gantry should distinguish "didn't know it exists" from "knew it existed but misunderstood scope/responsibilities".
- Three of the WRONG/PARTIAL functions are in subsystems the user said they didn't author (admin/posting workflow, events check-in). If gantry's audit is run by a team rather than a solo dev, the unrecognized-function rate should rise — worth testing on a multi-author repo.

---


---
