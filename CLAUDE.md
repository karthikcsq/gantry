# gantry — Project Context for Claude Code

All project context lives in **[AGENTS.md](./AGENTS.md)** — the single canonical,
agent-neutral entry point. Read it before touching the code.

@AGENTS.md

It covers: what gantry is (one `/gantry` skill, three working-tree modes —
forward / continue / rebuild), the authoritative CEO plan and how to keep the
"Latest plan / design doc" pointer fresh when gstack generates new artifacts,
skill routing, and project conventions.

Do not duplicate that content here. If something is Claude-Code-specific, add it
below; otherwise update AGENTS.md so every agent sees the same source of truth.
